# 慧标系统：开发环境、运行与部署说明

本文用于在新电脑上拉取代码、配置开发环境、运行慧标系统，以及按需启用智能预标注功能。

## 1. 系统组成

项目分为两个部分：

| 模块 | 用途 | 是否必须 |
| --- | --- | --- |
| 慧标系统主项目 | 标注平台、任务管理、审核、前端页面 | 必须 |
| ML Backend | 使用 Faster R-CNN 生成智能预标注结果 | 可选 |

如果只开发或演示标注平台，只需运行主项目。

如果需要使用“智能预标注”按钮，还需要启动 ML Backend。

## 2. 前置软件

建议安装：

1. Git
2. Anaconda 或 Miniconda
3. Docker Desktop
4. Node.js，仅在修改前端代码时需要

主项目后端使用 Python `3.8.20`。

## 3. 拉取主项目代码

```powershell
git clone https://github.com/GauntWheelCake/label-studio-master.git
cd label-studio-master
git switch Userfriendly-intelligent-annotation-development
```

后续获取最新代码：

```powershell
git pull
```

## 4. 配置 Conda 开发环境

不建议直接复制他人的 Conda 目录。Conda 环境中可能包含与本项目无关的 CUDA、PyTorch 和本机路径信息。

在主项目根目录中执行：

```powershell
conda create -n labelstudio python=3.8.20 -y
conda activate labelstudio

python -m pip install -r .\deploy\requirements.txt
python -m pip install "djangorestframework==3.13.1"
python -m pip install -e .
```

首次运行前执行数据库迁移：

```powershell
python .\label_studio\manage.py migrate
```

## 5. 原生开发模式运行主项目

收集静态文件：

```powershell
python .\label_studio\manage.py collectstatic --no-input
```

普通开发模式：

```powershell
python .\label_studio\manage.py runserver
```

浏览器访问：

```text
http://127.0.0.1:8080
```

如果需要同时连接运行在 Docker 中的 ML Backend，建议使用：

```powershell
python .\label_studio\manage.py runserver 0.0.0.0:8080
```

## 6. 修改前端代码后的构建方法

仅在修改 `label_studio\frontend` 中的源码后执行：

```powershell
cd .\label_studio\frontend
npm.cmd ci
npx.cmd webpack

cd ..\..
python .\label_studio\manage.py collectstatic --no-input
python .\label_studio\manage.py runserver
```

Windows PowerShell 中优先使用 `npm.cmd` 和 `npx.cmd`，避免执行策略拦截 `npm.ps1`。

## 7. 使用 Docker Compose 部署主项目

如果目标是快速部署或演示，优先使用 Docker Compose。此方式不依赖 Conda 环境。

执行前请确认：

1. Docker Desktop 已经启动。
2. 首次构建时电脑可以访问网络。Docker 需要拉取 Ubuntu、Nginx、PostgreSQL 镜像并安装 Python 依赖。
3. 当前终端位于主项目根目录，即包含 `docker-compose.yml` 的目录。

首次启动：

```powershell
docker compose up -d --build
```

首次启动会自动创建 PostgreSQL 数据目录、执行数据库迁移并启动 Nginx。等待片刻后，检查三个容器是否均为 `Up`：

```powershell
docker compose ps --all
```

正常情况下应看到：

```text
app
db
nginx
```

浏览器访问：

```text
http://localhost:8080
```

查看服务状态：

```powershell
docker compose ps
```

查看主服务日志：

```powershell
docker compose logs -f app
```

如果网页无法打开，优先执行：

```powershell
docker compose ps --all
docker compose logs --tail 200 app
docker compose logs --tail 100 nginx
docker compose logs --tail 100 db
```

停止服务：

```powershell
docker compose down
```

主项目数据会保存在：

```text
mydata
postgres-data
```

如需保留已有数据，不要删除这两个目录，也不要执行：

```powershell
docker compose down -v
```

## 8. 可选：启用智能预标注

### 8.1 拉取 ML Backend 代码

在另一个目录执行：

```powershell
git clone https://github.com/GauntWheelCake/ml_integration_v2.git
cd ml_integration_v2
```

### 8.2 获取 CPU 镜像

ML Backend 的模型权重较大，不存放在 GitHub 中。

请单独获取以下文件：

```text
ml-backend-cpu-0512.tar
```

文件大小约为 `487 MB`。

校验 SHA256：

```text
136D5D40A2BAE0C4609121063626BB38C436D20D8D5129F852CA11D9CA534A1B
```

导入镜像：

```powershell
docker load -i .\ml-backend-cpu-0512.tar
```

导入后镜像名称应为：

```text
ml-backend:cpu-0512
```

### 8.3 准备 ML Backend 配置

在 `ml_integration_v2` 根目录创建 `.env` 文件：

```env
LABEL_STUDIO_URL=http://host.docker.internal:8080
LABEL_STUDIO_API_TOKEN=<请通过私下渠道获取>
ML_BACKEND_PORT=9091
ML_BACKEND_URL=http://host.docker.internal:9091
MODEL_VERSION=faster-rcnn-resnet50-v1.0
MODEL_VERSION_OPTIONS=faster-rcnn-resnet50-v1.0
CONFIDENCE_THRESHOLD=0.7
IMAGE_REQUEST_TIMEOUT=20
INFERENCE_DEVICE=cpu
```

说明：

| 配置项 | 含义 |
| --- | --- |
| `LABEL_STUDIO_URL` | ML Backend 容器访问慧标系统的地址 |
| `LABEL_STUDIO_API_TOKEN` | 慧标系统 API Token，请勿提交到公开仓库。该值需要与主项目使用的 Token 一致 |
| `ML_BACKEND_PORT` | ML Backend 对外端口 |
| `ML_BACKEND_URL` | 慧标系统访问 ML Backend 时使用的地址 |
| `INFERENCE_DEVICE` | CPU 离线镜像固定使用 `cpu` |

### 8.4 启动 ML Backend

```powershell
docker run -d `
  --name ml-backend `
  -p 9091:9091 `
  --env-file .\.env `
  ml-backend:cpu-0512
```

如已存在同名容器，先删除旧容器：

```powershell
docker rm -f ml-backend
```

检查运行状态：

```powershell
docker logs -f ml-backend
```

浏览器访问：

```text
http://localhost:9091/health
http://localhost:9091/console
```

健康检查正常时，应返回类似内容：

```json
{
  "status": "ok"
}
```

## 9. 在慧标系统中连接 ML Backend

进入项目设置中的“智能标注”页面，连接模型服务。

根据主项目运行方式填写地址：

| 主项目运行方式 | ML Backend 地址 |
| --- | --- |
| 主项目使用 Docker Compose | `http://host.docker.internal:9091` |
| 主项目使用 Conda 原生运行 | `http://127.0.0.1:9091` |

注意：容器中的 `localhost` 只指向当前容器自身。容器访问宿主机服务时，应使用：

```text
host.docker.internal
```

## 10. 常用命令

### 主项目：Conda 开发模式

```powershell
conda activate labelstudio
python .\label_studio\manage.py runserver
```

### 主项目：Docker Compose

```powershell
docker compose up -d --build
docker compose logs -f app
docker compose down
```

### ML Backend

```powershell
docker start ml-backend
docker stop ml-backend
docker logs -f ml-backend
docker rm -f ml-backend
```

## 11. 交付清单

至少提供：

1. 主项目 GitHub 地址和分支名
2. 本文档

如需智能预标注功能，额外提供：

1. ML Backend GitHub 地址
2. `ml-backend-cpu-0512.tar`
3. API Token，通过私下渠道单独发送

## 12. 安全提醒

本地演示可以使用固定 Token。

如果部署到公网或服务器，请重新生成 Token，并避免将真实 Token 写入公开仓库、公开文档或聊天记录。
